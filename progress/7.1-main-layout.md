# 7.1 Main Layout

## Status
COMPLETED

## Summary
Three-panel layout built in page.tsx: FileBrowser sidebar (left, 240px), EditorPanel (center, flex), TerminalPanel (bottom, toggleable). Ctrl+` toggles the terminal.

## Description
Create a three-panel layout: sidebar (file browser) on the left, center area (editor with tabs), and bottom panel (terminal). All panels should be resizable. Ctrl+` toggles the terminal panel.

## Files Involved
| Destination (cushion) | Description |
|---|---|
| `apps/frontend/src/app/page.tsx` | Three-panel layout with sidebar, editor, and terminal |

## Acceptance Criteria
- [x] Three-panel layout renders correctly
- [x] Sidebar displays the file browser component
- [x] Center area displays the editor with a tab bar
- [x] Bottom panel displays the terminal
- [x] Ctrl+` toggles the terminal panel visibility

## Notes
- Layout assembled directly in page.tsx rather than separate layout components.
