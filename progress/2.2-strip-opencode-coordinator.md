# 2.2 Strip OpenCode from Coordinator

## Status
COMPLETED

## Description
Remove the `src/opencode/` folder entirely. From `server.ts`, remove: all `opencode/*` handlers, chat handlers, the permission system, session management, and streaming events. Keep document tracking, workspace handlers, and terminal handlers.

## Files Involved
| Action | File |
|---|---|
| DELETE | `apps/coordinator/src/opencode/` (entire folder) |
| EDIT | `apps/coordinator/src/server.ts` |
| DELETE | Any imports referencing removed modules |

## Acceptance Criteria
- [ ] `src/opencode/` folder no longer exists
- [ ] `server.ts` has no opencode handler registrations
- [ ] No chat, permission, or session management code remains
- [ ] Workspace handlers still present and intact
- [ ] Terminal handlers still present and intact
- [ ] Document tracking (`didOpen`, `didChange`) still present
- [ ] No dangling imports or references to removed code

## Notes
- Done as part of 2.1. The opencode/ folder was never copied. server.ts was written clean without any OpenCode handlers.
