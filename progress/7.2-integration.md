# 7.2 Integration

## Status
COMPLETED

## Summary
page.tsx wires: coordinator client connection on mount, workspace modal on first load, file open from sidebar to editor tabs, terminal panel toggle. Also created lib/utils.ts (cn utility) and hooks/useTerminal.ts.

## Description
Wire everything together: clicking a file in the browser opens a tab and loads the file in the editor, which syncs with the store. Terminal toggling works. Workspace modal appears on launch if no workspace is selected.

## Files Involved
| File | Role |
|---|---|
| `apps/frontend/src/app/page.tsx` | Main orchestration page |
| `apps/frontend/src/components/workspace/FileBrowser.tsx` | File selection events |
| `apps/frontend/src/components/editor/EditorPanel.tsx` | Display file content |
| `apps/frontend/src/stores/workspaceStore.ts` | State management |
| `apps/frontend/src/components/workspace/WorkspaceModal.tsx` | Initial workspace selection |
| `apps/frontend/src/lib/utils.ts` | cn utility (new) |
| `apps/frontend/src/hooks/useTerminal.ts` | Terminal hook (new) |

## Acceptance Criteria
- [x] Clicking a file in the browser opens it in a new tab
- [x] Selecting an already-open tab switches the editor content
- [x] Closing a tab removes it and switches to the next tab
- [x] Terminal panel toggles with Ctrl+`
- [x] Workspace modal appears on first launch (no workspace selected)
- [x] Selecting a workspace loads the file tree
- [x] All state changes are reflected in the workspace store

## Notes
- This is the critical integration task that ties all components together.
