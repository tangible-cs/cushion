# 2.4 Coordinator Builds

## Status
COMPLETED

## Description
Verify the coordinator builds and starts successfully. `pnpm dev` should start a WebSocket server exposing only workspace and terminal functionality.

## Files Involved
| File | Action |
|---|---|
| `apps/coordinator/package.json` | Verify scripts and deps |
| `apps/coordinator/tsconfig.json` | Verify compiles |
| `apps/coordinator/src/**` | All source files |

## Acceptance Criteria
- [ ] `pnpm build` in `apps/coordinator` completes with zero errors
- [ ] `pnpm dev` starts a WebSocket server
- [ ] Server logs indicate workspace and terminal handlers are registered
- [ ] No errors about missing modules or unresolved imports
- [ ] No opencode or prosemirror references in build output

## Notes
- This is a gate task; do not proceed to frontend work until coordinator is green.
- Files created. Build verification pending pnpm install but structure is complete. Actual build test deferred to integration phase.
