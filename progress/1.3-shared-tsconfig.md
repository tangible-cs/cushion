# 1.3 Shared TSConfig

## Status
COMPLETED

## Description
Create `packages/tsconfig` with shared TypeScript configuration presets: `node.json` for the coordinator (Node.js target) and `nextjs.json` for the frontend (Next.js target).

## Files Involved
| Source (doc-ai) | Destination (cushion) |
|---|---|
| `packages/tsconfig/node.json` | `packages/tsconfig/node.json` |
| `packages/tsconfig/nextjs.json` | `packages/tsconfig/nextjs.json` |
| `packages/tsconfig/package.json` | `packages/tsconfig/package.json` |

## Acceptance Criteria
- [ ] `packages/tsconfig/node.json` exists with Node.js-appropriate settings
- [ ] `packages/tsconfig/nextjs.json` exists with Next.js-appropriate settings
- [ ] Both configs can be extended by consumer `tsconfig.json` files
- [ ] Package named `@cushion/tsconfig`

## Notes
- Keep configs minimal; only include settings that differ from defaults.
- Created packages/tsconfig/package.json (@cushion/tsconfig).
- Created base.json (ES2022, NodeNext, strict, declaration, sourceMap), node.json (extends base, outDir dist, rootDir src), nextjs.json (extends base, jsx preserve, dom libs, bundler module resolution, next plugin).
