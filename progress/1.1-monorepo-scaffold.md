# 1.1 Monorepo Scaffold

## Status
COMPLETED

## Description
Create the monorepo root structure: `package.json`, `pnpm-workspace.yaml`, and a root `tsconfig.json`. Copy the base configuration from doc-ai and strip all tiptap and opencode references.

## Files Involved
| Source (doc-ai) | Destination (cushion) |
|---|---|
| `package.json` | `package.json` |
| `pnpm-workspace.yaml` | `pnpm-workspace.yaml` |
| `tsconfig.json` | `tsconfig.json` |

## Acceptance Criteria
- [ ] Root `package.json` exists with correct name (`cushion`) and workspace config
- [ ] `pnpm-workspace.yaml` defines `packages/` and `apps/` directories
- [ ] Root `tsconfig.json` sets up project references
- [ ] No references to tiptap or opencode remain in any root config file
- [ ] `pnpm install` succeeds at the root

## Notes
- This is the foundation; all subsequent tasks depend on this being correct.
- Created package.json (name: "cushion", scripts: dev/build/type-check/lint/clean, devDeps: concurrently + typescript, engines: node>=20 pnpm>=9, scope @cushion/*).
- Created pnpm-workspace.yaml (apps/* + packages/*).
- Created tsconfig.json (ES2022, NodeNext, strict, composite, project references to apps and packages).
