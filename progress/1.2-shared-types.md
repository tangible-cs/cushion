# 1.2 Shared Types

## Status
COMPLETED

## Description
Create `packages/types` with shared TypeScript type definitions: `FileTreeNode`, `WorkspaceMetadata`, `Range`, `Position`, `TextEdit`, `WorkspaceEdit`, terminal types, and coordinator RPC types. Must have zero dependencies on ProseMirror or OpenCode.

## Files Involved
| Source (doc-ai) | Destination (cushion) |
|---|---|
| `packages/types/src/*` | `packages/types/src/*` |
| `packages/types/package.json` | `packages/types/package.json` |
| `packages/types/tsconfig.json` | `packages/types/tsconfig.json` |

## Acceptance Criteria
- [ ] `packages/types` package exists with `@cushion/types` name
- [ ] Exports: `FileTreeNode`, `WorkspaceMetadata`, `Range`, `Position`, `TextEdit`, `WorkspaceEdit`
- [ ] Exports terminal-related types
- [ ] Exports coordinator RPC types (request/response/notification)
- [ ] No ProseMirror or OpenCode dependencies or type references
- [ ] Package builds cleanly with `tsc`

## Notes
- These types are consumed by both coordinator and frontend packages.
- Created packages/types/package.json (@cushion/types), tsconfig.json (standalone, ES2022, NodeNext, strict).
- Created src/index.ts with: Position, Range, TextEdit, WorkspaceEdit, FileTreeNode, WorkspaceMetadata, FileState, TabState, WorkspacePreferences, FileWatcherState, WorkspaceState, DidOpenTextDocumentParams, DidChangeTextDocumentParams, CodeActionParams, JSONRPCRequest, JSONRPCResponse, JSONRPCNotification, TerminalCommandParams, TerminalOutputParams, TerminalCreateParams, TerminalInputParams, TerminalResizeParams, DocumentState.
- No ProseMirror/OpenCode types included.
