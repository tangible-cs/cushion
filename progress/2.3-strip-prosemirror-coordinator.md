# 2.3 Strip ProseMirror from Coordinator

## Status
COMPLETED

## Description
Remove `utils/markdown-converter.ts` and the `textDocument/codeAction` handler. Keep `textDocument/didOpen` and `textDocument/didChange` for plain text document tracking.

## Files Involved
| Action | File |
|---|---|
| DELETE | `apps/coordinator/src/utils/markdown-converter.ts` |
| EDIT | `apps/coordinator/src/server.ts` (remove codeAction handler) |

## Acceptance Criteria
- [ ] `markdown-converter.ts` deleted
- [ ] `textDocument/codeAction` handler removed from `server.ts`
- [ ] `textDocument/didOpen` handler still works
- [ ] `textDocument/didChange` handler still works
- [ ] No ProseMirror imports remain anywhere in coordinator

## Notes
- Plain text tracking is needed for future editor sync.
- Done as part of 2.1. markdown-converter.ts was never copied. textDocument/codeAction handler excluded.
