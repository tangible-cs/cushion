# 3.3 Workspace Store

## Status
COMPLETED

## Description
Copy `stores/workspaceStore.ts`. Strip all OpenCode context calls. Keep: workspace metadata, file list, tabs, recent projects, preferences, and persistence logic.

## Files Involved
| Source (doc-ai) | Destination (cushion) |
|---|---|
| `apps/frontend/src/stores/workspaceStore.ts` | `apps/frontend/src/stores/workspaceStore.ts` |

## Acceptance Criteria
- [ ] Store file copied and cleaned
- [ ] Workspace metadata (name, path, etc.) management works
- [ ] File list state management works
- [ ] Tab state (open, close, switch, reorder) works
- [ ] Recent projects list works
- [ ] User preferences persist across sessions
- [ ] No OpenCode context or session references remain

## Notes
- Uses Zustand + persist. Workspace metadata, open files, tabs, recent projects, preferences. Persist key changed to "cushion-workspace". Stripped OpenCode context calls.
